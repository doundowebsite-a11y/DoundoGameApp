import React from 'react';
import Svg, { Rect, Path } from 'react-native-svg';

export const IconCardBack = ({ size = 24, color = '#256af4' }) => (
  // Providing a stylized card back if the SVG isn't cleanly loading as a React component.
  // Actually, let's just use the SVG file.
  null
);
